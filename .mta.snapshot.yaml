_schema-version: '3.1'
ID: zfull
version: 1.0.0
description: A simple CAP project.
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
  subdomain: d9d4a892trial
build-parameters:
  before-all:
    - builder: custom
      commands:
        - rm -rf build/dist
        - mkdir -p build/dist
        - rm -rf build/ui-deployer
        - mkdir -p build/ui-deployer
    - builder: custom
      commands:
        - npm install --production
        - >-
          bash -c "cds -v | grep 'cds-dk' || npm i --production --no-save
          @sap/cds-dk"
        - bash -c "export PATH=$PATH:$(pwd)/node_modules/.bin"
        - cds build --production
  after-all:
    - builder: custom
      commands:
        - npm install
modules:
  - name: zfull-zfull.stores
    type: html5
    path: app/zfull.stores
    build-parameters:
      builder: custom
      commands:
        - rm -rf dist-zip dist
        - npm install
        - >-
          npx -p @ui5/cli@^3.0.0 ui5 build --dest dist
          --include-task=generateCachebusterInfo
        - mkdir dist-zip
        - bash -c "cd dist && npx bestzip ../dist-zip/zfullstores.zip *"
      _artifactName: zfullstores.zip
      supported-platforms: []
      build-result: dist-zip
  - name: zfull-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm
      ignore:
        - node_modules
        - package-lock.json
    requires:
      - name: zfull-service-uaa
      - name: zfull-service-db
  - name: zfull-srv
    type: nodejs
    path: gen/srv
    parameters:
      memory: 256MB
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    build-parameters:
      builder: npm
      ignore:
        - node_modules
        - package-lock.json
    requires:
      - name: zfull-service-uaa
      - name: zfull-service-db
      - name: zfull-service-destination
      - name: zfull-service-metadata
      - name: zfull-service-connectivity
      - name: zfull-service-logs
  - name: zfull-aux-destinations
    type: com.sap.application.content
    parameters:
      content:
        subaccount:
          existing_destinations_policy: update
          destinations:
            - Authentication: OAuth2UserTokenExchange
              Name: zfull-service-uaa
              ServiceInstanceName: zfull-uaa
              ServiceKeyName: zfull-uaa-key
              sap.cloud.service: lcap.zfull
            - Authentication: OAuth2UserTokenExchange
              Name: zfull-app-srv
              TokenServiceInstanceName: zfull-uaa
              TokenServiceKeyName: zfull-uaa-key
              URL: ~{srv-api/srv-url}
              sap.cloud.service: lcap.zfull
            - Name: zfull-service-html5-apps-repo-host
              ServiceInstanceName: zfull-html5-apps-repo-host
              ServiceKeyName: zfull-html5-apps-repo-host-key
              sap.cloud.service: lcap.zfull
            - Authentication: OAuth2UserTokenExchange
              Name: zfull-zfullSrv
              TokenServiceInstanceName: zfull-uaa
              TokenServiceKeyName: zfull-uaa-key
              URL: ~{srv-api/srv-url}/service/zfull
              sap.cloud.service: lcap.zfull
              sap.applicationdevelopment.actions.enabled: false
              sap.build.usage: CAP
              sap.processautomation.enabled: false
              HTML5.DynamicDestination: false
              WebIDEEnabled: false
              MobileEnabled: false
              AppgyverEnabled: false
    build-parameters:
      no-source: true
    requires:
      - name: zfull-service-uaa
        parameters:
          service-key:
            name: zfull-uaa-key
      - name: srv-api
      - name: zfull-service-html5-apps-repo-host
        parameters:
          service-key:
            name: zfull-html5-apps-repo-host-key
      - name: zfull-service-destination
        parameters:
          content-target: true
  - name: zfull-launchpad
    type: html5
    path: build/launchpad
    build-parameters:
      builder: custom
      commands:
        - npm install
        - >-
          npx -p @ui5/cli@^3.0.0 ui5 build --dest dist
          --include-task=generateCachebusterInfo
        - bash -c "cd dist && npx bestzip ../../../build/dist/launchpad.zip *"
        - rm -rf dist
      _artifactName: launchpad.zip
      supported-platforms: []
      build-result: ../../build/dist
  - name: zfull-aux-ui-deployer
    type: com.sap.application.content
    path: build/ui-deployer
    build-parameters:
      build-result: resources
      requires:
        - name: zfull-zfull.stores
          artifacts:
            - zfullstores.zip
          target-path: resources
        - name: zfull-launchpad
          artifacts:
            - launchpad.zip
          target-path: resources
    requires:
      - name: zfull-service-html5-apps-repo-host
        parameters:
          content-target: true
resources:
  - type: org.cloudfoundry.managed-service
    name: zfull-service-uaa
    parameters:
      service: xsuaa
      service-plan: application
      service-name: zfull-uaa
      path: xs-security.json
      config:
        oauth2-configuration:
          redirect-uris:
            - https://${subdomain}.launchpad.${default-domain}/login/*
            - https://${subdomain}.${default-domain}/login/*
          credential-types:
            - binding-secret
            - x509
        xsappname: zfull-${space}
        role-collections:
          - description: Admin
            name: zfull-Admin-${space}
            role-template-references:
              - $XSAPPNAME.Admin
          - description: Read
            name: zfull-Read-${space}
            role-template-references:
              - $XSAPPNAME.Read
  - type: org.cloudfoundry.managed-service
    name: zfull-service-db
    parameters:
      service: hana
      service-plan: hdi-shared
      service-name: zfull-db
  - type: org.cloudfoundry.managed-service
    name: zfull-service-destination
    parameters:
      service: destination
      service-plan: lite
      service-name: zfull-destination
      config:
        HTML5Runtime_enabled: true
        version: 1.0.0
  - type: org.cloudfoundry.user-provided-service
    name: zfull-service-metadata
    parameters:
      service: metadata
      service-plan: standard
      service-name: zfull-metadata
      config:
        default-domain: ${default-domain}
  - type: org.cloudfoundry.managed-service
    name: zfull-service-connectivity
    parameters:
      service: connectivity
      service-plan: lite
      service-name: zfull-connectivity
  - type: org.cloudfoundry.managed-service
    name: zfull-service-logs
    parameters:
      service: application-logs
      service-plan: lite
      service-name: zfull-logs
  - type: org.cloudfoundry.managed-service
    name: zfull-service-html5-apps-repo-host
    parameters:
      service: html5-apps-repo
      service-plan: app-host
      service-name: zfull-html5-apps-repo-host
